import React from 'react';
import Hero from '../components/Hero';

export default function Landing(props) {
    return (
        <React.Fragment>
            <Hero/>
        </React.Fragment>

    )
}